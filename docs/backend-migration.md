# Rooms: переход от frontend-прототипа к backend

Статус: технический план для MVP. Документ не означает, что production backend уже подключён.

## Текущее состояние

- установлен Node.js 24 LTS;
- создан `server` на Fastify и TypeScript;
- работают `/health`, `/v1/cities`, `/v1/cities/{cityId}/stats`, `/v1/rooms`, `/v1/rooms/{roomId}`, `/v1/rooms/{roomId}/reviews` и `/v1/availability/search`;
- публичный каталог, фильтры и пересечение доступности покрыты автоматическими тестами;
- готовы `MemoryCatalogRepository` и `PostgresCatalogRepository` с единым HTTP-контрактом;
- добавлены контролируемая первоначальная миграция и защищённое демо-наполнение;
- PostgreSQL 17.10 установлен как автоматическая служба Windows;
- схема `database.sql` применена миграцией `0001_initial`, демо-каталог загружен;
- миграция `0002_booking_conversations` добавила предложения времени и серверные отметки прочтения переписки;
- миграция `0003_partner_catalog` добавила комментарии к исключениям графика и индекс календаря площадки;
- миграция `0004_auth_security` добавила одноразовые токены восстановления и отметку активности сессий;
- миграция `0005_notification_worker` добавила защищённую очередь внешних уведомлений;
- миграция `0006_partner_photos` добавила статусы модерации, ключи хранения и технические метаданные фотографий;
- фотографии могут храниться в приватном S3-совместимом bucket; API выдаёт их через стабильные `/media/*` URL и удаляет частичные загрузки при сбое;
- миграция `0007_reviews` добавила комментарий модератора, автора решения, даты модерации и индексы очереди отзывов;
- миграция `0008_support_and_admin_queue` добавила обращения по броням, сообщения участников и индексы очереди поддержки Rooms;
- миграция `0009_finance_operations` добавила безопасные метаданные реквизитов, идентификаторы банковских операций, индексы финансовых очередей и исправила исторический расчёт суммы партнёру;
- миграция `0010_payout_destination_snapshot` отделила назначение уже сформированной выплаты от последующих изменений реквизитов партнёра;
- локальный API использует `postgresql`; `memory` остаётся только режимом разработки без `DATABASE_URL`;
- frontend-каталог и опубликованные отзывы подключаются к API в локальном preview с автоматическим fallback;
- регистрация клиента, вход по email/телефону, `argon2id`, короткий access token и ротация refresh cookie работают через PostgreSQL;
- восстановление пароля использует 15-минутный одноразовый токен, а клиент видит и завершает активные сессии;
- профиль клиента и заявки синхронизируются с PostgreSQL; цена, услуги, предоплата и остаток рассчитываются сервером;
- партнёр входит с серверной ролью, видит заявки только своей площадки, подтверждает или отклоняет их через API;
- ручные брони и техническая занятость партнёра синхронизируются с PostgreSQL, блокируют публичную доступность и записываются в аудит;
- подтверждение атомарно создаёт 15-минутное удержание всех помещений, а истечение срока освобождает слот;
- платёжный intent создаётся только сервером по сохранённой сумме предоплаты;
- локальная демо-оплата идемпотентно превращает удержание в постоянный резерв и открывает контакты партнёру;
- партнёр предлагает проверенное общее окно, клиент принимает или отклоняет его, а сумма и новое удержание рассчитываются сервером;
- чат заявки хранится в PostgreSQL, считает непрочитанные сообщения и блокирует прямые контакты до предоплаты;
- заведение, помещения, дополнительные услуги, недельный график и исключения партнёра сохраняются через API в PostgreSQL;
- публичные изменения карточек остаются в `moderation_requests`, а контакты, буфер и расписание применяются сразу и записываются в аудит;
- администратор получает серверную очередь, сравнивает данные, принимает или отклоняет правки; решение и причина доступны партнёру;
- партнёр загружает JPEG, PNG и WebP до 12 МБ; сервер сохраняет исходную ориентацию и создаёт горизонтальную и вертикальную WebP-версии, а публикация проходит через общую очередь модерации;
- завершение оплаченной брони, отзыв клиента, модерация Rooms, пересчёт рейтинга и публичный ответ партнёра работают через PostgreSQL;
- администратор видит серверную очередь всех броней, а клиент и Rooms отменяют заявку транзакционно с освобождением резервов;
- отмена оплаченной брони создаёт отдельную задачу на возврат, а её состояние возвращается вместе со статусом оплаты;
- партнёр сохраняет зашифрованные реквизиты, бухгалтер проверяет их, обрабатывает возвраты и формирует выплаты, а партнёр видит только собственную историю;
- клиент, партнёр и администратор ведут обращения по брони через PostgreSQL; Rooms назначает обращению статус `open`, `working` или `closed`;
- GitHub Pages продолжает работать независимо как демонстрация, а backend-версия получает явную runtime-конфигурацию и в production не переходит на локальные кабинеты или брони.

## Цель

Перенести данные из `localStorage` в PostgreSQL, не меняя согласованные пользовательские сценарии:

- клиент выбирает конкретное помещение и свободный интервал;
- новая заявка не блокирует слот;
- подтверждённая заявка блокирует слот на 15 минут ожидания предоплаты;
- оплаченная бронь, ручная бронь, техническая занятость и буфер блокируют доступность;
- телефон клиента открывается партнёру только после предоплаты;
- критичные изменения карточки проходят модерацию;
- все финансовые и административные действия записываются в аудит.

## Рекомендуемый стек

- API: Node.js LTS + Fastify + TypeScript.
- База: PostgreSQL 16 или новее.
- Миграции и запросы: параметризованные запросы `pg`, репозитории и журнал контрольных сумм миграций. ORM можно добавить позже, если объём записывающих сценариев оправдает дополнительный слой.
- Очереди: Redis + BullMQ для email, Telegram, чеков, напоминаний и повторных попыток.
- Файлы: S3-совместимое хранилище с отдельными оригиналами и производными изображениями.
- Авторизация: короткоживущий access token и refresh token в `HttpOnly Secure` cookie.
- Платежи: серверный адаптер Сбер эквайринга, размещённая платёжная страница и webhook с независимой проверкой статуса заказа у банка.
- Чеки: серверная интеграция с онлайн-кассой и хранение фискальных реквизитов.
- Наблюдаемость: структурированные логи, защищённая административная сводка запросов и очередей уже реализованы; перед production остаётся подключить внешний error tracking и оповещения по порогам из `docs/operations.md`.

## Этапы

### 1. Каркас и чтение каталога

1. [Готово] Реализовать health endpoint и публичное чтение городов и помещений.
2. [Готово] Загрузить типизированные демо-данные Воронежа и Москвы.
3. [Готово] Поднять PostgreSQL и применить `database.sql`.
4. [Готово] Перенести каталог в PostgreSQL через репозиторий без изменения HTTP-маршрутов.
5. [Готово] Добавить серверное чтение одобренных отзывов без контактов клиента.
6. [Готово] Перевести frontend-каталог на API с локальным fallback только для разработки.

### 2. Авторизация и кабинеты

1. [Готово] Создать серверную регистрацию клиента и вход по email/телефону.
2. [Готово] Хранить только хеш пароля (`argon2id`), никогда не переносить пароли из прототипа.
3. [Готово] Реализовать роли `client`, `partner`, `admin`, `accountant` в серверной модели сессии.
4. [Готово] Перенести редактирование заведения, помещений, услуг и графика партнёра в PostgreSQL.
5. [Готово] Перенести очередь и решения модерации в PostgreSQL с атомарным применением правок и аудитом.
6. [Готово] Добавить восстановление пароля и отзыв всех активных сессий.

### 3. Доступность и бронь

1. [Готово] Чтение графика, исключений, существующих резервов и буферов работает; временные удержания, ручные брони и техническая занятость записываются сервером и используют единый механизм конфликтов.
2. [Готово] Перед созданием заявки сервер повторно проверяет общее окно, а сама заявка, помещения, услуги и согласие записываются одной транзакцией.
3. [Готово] При подтверждении создавать временное удержание слота на 15 минут и автоматически освобождать его после истечения срока.
4. [Готово на уровне адаптеров] При подтверждённой оплате превращать удержание в бронь; позднюю оплату направлять в возврат без повторного захвата слота.
5. [Готово для удержаний] Защититься от двойного подтверждения ограничением БД и последовательной блокировкой строк помещений.
6. [Готово] Согласовывать альтернативное время через сервер и повторно проверять слот при принятии клиентом.
7. [Готово] Хранить переписку и отметки прочтения; до оплаты блокировать передачу прямых контактов.
8. [Готово] Дать клиенту и Rooms отменять заявки с атомарным освобождением всех удержаний и резервов.
9. [Готово] Перенести обращения клиента и партнёра в общую серверную очередь Rooms.

### 4. Оплата и финансы

1. [Готово] Создавать платёж только на сервере по сумме, рассчитанной из сохранённого заказа.
2. [Готово] Обрабатывать webhook идемпотентно, сверять состояние и сумму у банка и не доверять одному входящему событию.
3. [Готово] Сохранять предоплату, комиссию Rooms, остаток на площадке и сумму партнёру; сумма выплаты равна предоплате за вычетом комиссии.
4. [Готово до production credentials] При отмене оплаченной брони формировать отдельную задачу на возврат, проверять реквизиты, блокировать спорные выплаты и вести пакетные выплаты с аудитом. Возврат Сбера обрабатывает долговечный worker: транзакционно захватывает задачу, проверяет уже возвращённую сумму, один раз отправляет `refund.do`, повторяет только проверку и завершает операцию после ответа банка. Чеки продажи и возврата обрабатывает отдельный worker с восстановлением зависших задач и ручным управлением из бухгалтерии. Далее подключить production credentials Сбера, адаптер выбранной онлайн-кассы и автоматическое банковское проведение выплат партнёрам.

### 5. Уведомления и файлы

1. [Готово] Перенести очередь из `rooms_notification_outbox_v30` в таблицу `notification_deliveries`; frontend использует её при подключённом API. Локальный fallback сохранён только для отдельной демонстрации GitHub Pages и отключён в backend production-режиме.
2. [Готово на уровне адаптеров] Подключить SMTP и Telegram Bot API через worker с шифрованием, дедупликацией, повторными попытками и очисткой отправленного тела. Для реальной отправки остаётся выдать production credentials.
3. [Готово] Загружать фото через защищённый multipart API, проверять фактический формат и размер.
4. [Готово] Создавать полноразмерную, горизонтальную и вертикальную WebP-версии без растяжения исходника.
5. [Готово на уровне адаптера] Хранить production-фотографии в приватном S3-совместимом bucket и выдавать их через API. Для запуска остаётся создать bucket, выдать минимальные права сервисному аккаунту и настроить versioning/backup.

## Импорт текущего прототипа

1. Войти в админку Rooms.
2. Открыть «Центр миграции данных».
3. Скачать JSON-снимок.
4. Проверить `format=rooms-migration`, `version=1` и контрольные количества коллекций.
5. Импортировать снимок серверной административной командой.
6. Создать приглашения на сброс пароля клиентам и партнёрам.
7. Сверить брони, оплаты, согласия и выплаты до переключения frontend на API.

Снимок намеренно не содержит паролей и активных сессий. Он содержит персональные и финансовые данные, поэтому должен передаваться только по защищённому каналу и удаляться после подтверждённого импорта.

## Обязательные проверки перед запуском

- параллельное подтверждение одного слота не создаёт две брони;
- повторный webhook не создаёт повторную оплату или чек;
- партнёр не видит телефон до предоплаты;
- клиент не видит комиссию Rooms;
- доступ к площадке ограничен её партнёром;
- все административные действия попадают в `audit_log`;
- полный расчётный счёт не возвращается из API, изменение реквизитов снимает проверку, а партнёр не видит выплаты другой площадки;
- согласия хранят версию документа, дату, субъект и контекст;
- резервное восстановление PostgreSQL проверено на отдельном окружении.
