openapi: 3.1.0
info:
  title: Rooms API
  version: 0.19.0
  description: |
    Contract for the Rooms MVP backend. GitHub Pages remains a static frontend;
    the local Fastify service provides PostgreSQL-backed API endpoints.
servers:
  - url: http://localhost:3001
    description: Local development
  - url: https://api.rooms.example
    description: Production placeholder
tags:
  - name: Public
  - name: Auth
  - name: Client
  - name: Partner
  - name: Admin
  - name: Payments
  - name: Accounting

paths:
  /health:
    get:
      tags: [Public]
      operationId: getHealth
      responses:
        '200':
          description: Service health
          content:
            application/json:
              schema:
                type: object
                required: [status, database, storage, media, rateLimits, payments, time]
                properties:
                  status: {type: string, const: ok}
                  database: {type: string, enum: [up, down]}
                  storage: {type: string, enum: [memory, postgresql]}
                  media: {type: string, enum: [memory, local, s3]}
                  rateLimits: {type: string, enum: [memory, postgresql]}
                  payments: {type: string, enum: [rooms_demo, sber]}
                  time: {type: string, format: date-time}

  /v1/cities:
    get:
      tags: [Public]
      operationId: listCities
      responses:
        '200':
          description: Supported cities
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/City'

  /v1/partner-leads:
    post:
      tags: [Public]
      operationId: createPartnerLead
      description: Stores a venue onboarding application. The response deliberately excludes contact data.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PartnerLeadCreate'
      responses:
        '201':
          description: Application accepted for review
          headers:
            Cache-Control: {schema: {type: string, const: no-store}}
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartnerLeadReceipt'
        '400':
          $ref: '#/components/responses/ValidationError'
        '409':
          description: An active application already uses this email or phone
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
        '429':
          description: Too many applications from one network address
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}

  /v1/auth/partner-invitations/preview:
    post:
      tags: [Auth]
      operationId: previewPartnerInvitation
      description: Validates an opaque invitation token sent in the request body so it is not written to API access logs.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PartnerInvitationToken'}
      responses:
        '200':
          description: Minimal approved venue context; contact email and phone are deliberately excluded
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerInvitationPreview'}
        '410':
          description: Invitation expired, was revoked or was already consumed
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
        '429':
          description: Too many validation attempts from this network address
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}

  /v1/auth/partner-invitations/accept:
    post:
      tags: [Auth]
      operationId: acceptPartnerInvitation
      description: Atomically creates the partner user, review-state venue, membership and legal consent, then consumes the invitation.
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PartnerInvitationAcceptance'}
      responses:
        '201':
          description: Partner account activated and authenticated
          headers:
            Set-Cookie:
              schema: {type: string}
              description: HttpOnly refresh cookie
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerActivationSession'}
        '202':
          description: Partner account activated; mandatory two-factor setup must be completed before a session is issued
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerActivationChallenge'}
        '400':
          $ref: '#/components/responses/ValidationError'
        '409':
          description: An account already exists for the approved contact
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
        '410':
          description: Invitation expired, was revoked or was already consumed
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}

  /v1/cities/{cityId}/stats:
    get:
      tags: [Public]
      operationId: getCityStats
      parameters:
        - name: cityId
          in: path
          required: true
          description: City id returned by /v1/cities or its exact display name.
          schema: {type: string}
      responses:
        '200':
          description: Public supply counts and privacy-safe audience bucket
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CityStats'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/rooms:
    get:
      tags: [Public]
      operationId: searchRooms
      parameters:
        - {name: city, in: query, required: true, schema: {type: string}}
        - {name: date, in: query, schema: {type: string, format: date}}
        - {name: time, in: query, schema: {type: string, pattern: '^([01][0-9]|2[0-3]):[0-5][0-9]$'}}
        - {name: durationMinutes, in: query, schema: {type: integer, minimum: 30, multipleOf: 30}}
        - {name: guests, in: query, schema: {type: integer, minimum: 1}}
        - {name: type, in: query, schema: {type: string}}
        - {name: features, in: query, schema: {type: array, items: {type: string}}, style: form, explode: false}
        - {name: maxPricePerHour, in: query, schema: {type: number, minimum: 0}}
        - {name: sort, in: query, schema: {type: string, enum: [rating, price, capacity], default: rating}}
        - {name: cursor, in: query, schema: {type: string}}
      responses:
        '200':
          description: Published rooms and optional availability summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoomSearchResult'
        '400':
          $ref: '#/components/responses/ValidationError'

  /v1/rooms/{roomId}:
    get:
      tags: [Public]
      operationId: getRoom
      parameters:
        - $ref: '#/components/parameters/RoomId'
        - {name: date, in: query, schema: {type: string, format: date}}
      responses:
        '200':
          description: Room detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoomDetail'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/rooms/{roomId}/reviews:
    get:
      tags: [Public]
      operationId: listRoomReviews
      parameters:
        - $ref: '#/components/parameters/RoomId'
      responses:
        '200':
          description: Approved reviews for a published room with privacy-safe author names
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicReviewPage'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/availability/search:
    post:
      tags: [Public]
      operationId: searchAvailability
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AvailabilityRequest'
      responses:
        '200':
          description: Free starts and maximum duration for each start
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailabilityResponse'
        '400':
          $ref: '#/components/responses/ValidationError'

  /v1/planning/preview:
    post:
      tags: [Public]
      operationId: previewPlanningVariants
      description: |
        Runs the experimental deterministic Rooms planner against current server-side
        room prices, schedules and reservation blocks. The client supplies room IDs,
        never prices or occupancy. Results are not cached and contain no personal data.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlanningPreviewRequest'
      responses:
        '200':
          description: Ranked and explained booking variants
          headers:
            Cache-Control: {schema: {type: string, const: no-store}}
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlanningPreviewResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '404':
          $ref: '#/components/responses/NotFound'
        '429':
          $ref: '#/components/responses/RateLimited'

  /v1/auth/client/register:
    post:
      tags: [Auth]
      operationId: registerClient
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClientRegistration'
      responses:
        '201':
          description: Client account and session created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSession'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/auth/login:
    post:
      tags: [Auth]
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [login, password]
              properties:
                login: {type: string}
                password: {type: string, minLength: 1}
      responses:
        '200':
          description: Authenticated session
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSession'
        '202':
          description: Password accepted; the protected role must complete a short-lived two-factor challenge
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TwoFactorChallenge'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/auth/2fa/complete:
    post:
      tags: [Auth]
      operationId: completeTwoFactorLogin
      description: Validates a TOTP or one-time recovery code and only then creates the authenticated session.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [challengeToken, code]
              properties:
                challengeToken: {type: string, minLength: 32, maxLength: 200, writeOnly: true}
                code: {type: string, minLength: 6, maxLength: 24, writeOnly: true}
      responses:
        '200':
          description: Two-factor check completed and session created
          headers:
            Set-Cookie:
              schema: {type: string}
              description: HttpOnly refresh cookie
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TwoFactorSession'}
        '401':
          $ref: '#/components/responses/Unauthorized'
        '410':
          description: Challenge expired, was consumed or reached the attempt limit
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
        '429':
          description: Too many attempts from the network address
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}

  /v1/auth/2fa/recovery/request:
    post:
      tags: [Auth]
      operationId: requestTwoFactorRecovery
      description: Consumes a verify-mode challenge created only after a correct password and opens a 24-hour administrator review request. It never disables the existing factor by itself.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [challengeToken]
              properties:
                challengeToken: {type: string, minLength: 32, maxLength: 200, writeOnly: true}
      responses:
        '202':
          description: Recovery request accepted; no authenticated session is created
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TwoFactorRecoveryAccepted'}
        '410':
          description: Challenge is unavailable, was a setup challenge or was already consumed
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
        '429':
          description: Too many recovery requests from the network address
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}

  /v1/auth/password-reset/request:
    post:
      tags: [Auth]
      operationId: requestPasswordReset
      description: Always returns the same accepted response so an account cannot be discovered by login enumeration.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [login]
              properties:
                login: {type: string, minLength: 3, maxLength: 254}
      responses:
        '202':
          description: Reset delivery accepted regardless of whether the account exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetAccepted'
        '429':
          $ref: '#/components/responses/ValidationError'

  /v1/auth/password-reset/confirm:
    post:
      tags: [Auth]
      operationId: confirmPasswordReset
      description: Consumes a one-time reset token, changes the password and revokes every existing session.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, newPassword]
              properties:
                token: {type: string, minLength: 32, maxLength: 200}
                newPassword: {type: string, minLength: 8, maxLength: 128}
      responses:
        '204': {description: Password changed and existing sessions revoked}
        '400':
          $ref: '#/components/responses/ValidationError'

  /v1/auth/refresh:
    post:
      tags: [Auth]
      operationId: refreshSession
      description: Rotates the HttpOnly refresh cookie and returns a new access token.
      security:
        - refreshCookie: []
      responses:
        '200':
          description: Session refreshed and refresh cookie rotated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSession'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/auth/logout:
    post:
      tags: [Auth]
      operationId: logout
      security:
        - bearerAuth: []
        - refreshCookie: []
      responses:
        '204': {description: Session revoked}

  /v1/me:
    get:
      tags: [Client]
      operationId: getCurrentUser
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/Unauthorized'
    patch:
      tags: [Client]
      operationId: updateCurrentUser
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClientProfileUpdate'
      responses:
        '200':
          description: Updated client profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/me/two-factor:
    get:
      tags: [Client]
      operationId: getCurrentUserTwoFactorStatus
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Two-factor requirement and enrollment state for the current account
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TwoFactorStatus'}
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/me/sessions:
    get:
      tags: [Client]
      operationId: listCurrentUserSessions
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Active sessions owned by the current user
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items: {$ref: '#/components/schemas/UserSession'}
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/me/sessions/others:
    delete:
      tags: [Client]
      operationId: revokeOtherCurrentUserSessions
      security:
        - bearerAuth: []
      responses:
        '204': {description: Every active session except the current one was revoked}
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/me/sessions/{sessionId}:
    delete:
      tags: [Client]
      operationId: revokeCurrentUserSession
      security:
        - bearerAuth: []
      parameters:
        - name: sessionId
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '204': {description: Owned session revoked}
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/me/notification-settings:
    get:
      tags: [Client]
      operationId: getNotificationSettings
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Notification channels
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationSettings'
    patch:
      tags: [Client]
      operationId: updateNotificationSettings
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationSettings'
      responses:
        '200':
          description: Updated channels
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationSettings'

  /v1/me/notification-settings/test:
    post:
      tags: [Client]
      operationId: testNotificationSettings
      security:
        - bearerAuth: []
      responses:
        '202':
          description: Test deliveries accepted by the server queue
          content:
            application/json:
              schema:
                type: object
                required: [accepted, deliveries]
                properties:
                  accepted: {type: boolean, const: true}
                  deliveries:
                    type: array
                    items:
                      $ref: '#/components/schemas/NotificationDelivery'
        '409':
          description: All external channels are disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/me/notification-deliveries:
    get:
      tags: [Client]
      operationId: listOwnNotificationDeliveries
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [queued, processing, sent, failed, cancelled]}}
        - {name: channel, in: query, schema: {type: string, enum: [email, telegram]}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 50}}
      responses:
        '200':
          description: Masked delivery history for the authenticated account
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/NotificationDelivery'
  /v1/me/reviews:
    get:
      tags: [Client]
      operationId: listCurrentClientReviews
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Reviews submitted by the authenticated client, including moderation feedback
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/ReviewRecord'}
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/me/support:
    get:
      tags: [Client]
      operationId: listCurrentClientSupportCases
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [open, working, closed, all]}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Support cases for bookings owned by the authenticated client
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/SupportCase'}
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/bookings:
    get:
      tags: [Client]
      operationId: listClientBookings
      security:
        - bearerAuth: []
      parameters:
        - {name: statusGroup, in: query, schema: {type: string, enum: [active, completed, cancelled, all]}}
      responses:
        '200':
          description: Client bookings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Booking'
    post:
      tags: [Client]
      operationId: createBooking
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BookingCreate'
      responses:
        '201':
          description: New request. The slot is not blocked yet.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
        '409':
          $ref: '#/components/responses/SlotConflict'

  /v1/bookings/{bookingId}:
    get:
      tags: [Client]
      operationId: getBooking
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      responses:
        '200':
          description: Booking visible to the current role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/bookings/{bookingId}/cancel:
    post:
      tags: [Client]
      operationId: cancelBooking
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: {type: string, minLength: 3, maxLength: 1000}
      responses:
        '200':
          description: Cancelled booking or refund review created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'

  /v1/bookings/{bookingId}/complete:
    post:
      tags: [Client]
      operationId: completeClientBooking
      description: Marks a paid booking as completed after its end time and unlocks review submission.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      responses:
        '200':
          description: Completed booking
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Booking'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/bookings/{bookingId}/review:
    post:
      tags: [Client]
      operationId: submitBookingReview
      description: Creates one pending review for a completed booking, or resubmits its rejected review.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ReviewSubmit'}
      responses:
        '201':
          description: Review queued for moderation
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ReviewRecord'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/bookings/{bookingId}/messages:
    get:
      tags: [Client, Partner]
      operationId: listBookingMessages
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      responses:
        '200':
          description: Visible booking messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BookingMessage'
    post:
      tags: [Client, Partner]
      operationId: createBookingMessage
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body: {type: string, minLength: 1, maxLength: 1000}
      responses:
        '201':
          description: Message accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BookingMessage'
        '422':
          description: Direct contact details are blocked before payment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/bookings/{bookingId}/support:
    get:
      tags: [Client, Partner, Admin]
      operationId: listBookingSupportCases
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      responses:
        '200':
          description: Support history visible to the authenticated booking participant
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/SupportCase'}
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [Client, Partner, Admin]
      operationId: openBookingSupportCase
      description: Opens a case or appends the message to the current non-closed case for this booking.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [topic, body]
              properties:
                topic: {type: string, minLength: 3, maxLength: 200}
                body: {type: string, minLength: 5, maxLength: 3000}
      responses:
        '201':
          description: Support case created or updated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SupportCase'}
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/support/{supportId}/messages:
    post:
      tags: [Client, Partner, Admin]
      operationId: createSupportMessage
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SupportId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [body]
              properties:
                body: {type: string, minLength: 5, maxLength: 3000}
      responses:
        '201':
          description: Message appended to the support case
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SupportCase'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/bookings/{bookingId}/proposal/accept:
    post:
      tags: [Client]
      operationId: acceptBookingTimeProposal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [proposalId]
              properties:
                proposalId: {type: string, format: uuid}
      responses:
        '200':
          description: Proposal accepted and all booking rooms held for 15 minutes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Proposal changed or its slot is no longer available
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/bookings/{bookingId}/proposal/decline:
    post:
      tags: [Client]
      operationId: declineBookingTimeProposal
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [proposalId]
              properties:
                proposalId: {type: string, format: uuid}
      responses:
        '200':
          description: Proposal declined and request returned to the partner queue
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Proposal has already changed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/bookings/{bookingId}/payment-intent:
    post:
      tags: [Payments]
      operationId: createPaymentIntent
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      responses:
        '201':
          description: Server-calculated payment intent; an external provider returns a hosted payment page URL
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentIntent'
        '409':
          $ref: '#/components/responses/SlotConflict'

  /v1/payments/{paymentId}/demo-complete:
    post:
      tags: [Payments]
      operationId: completeDemoPayment
      description: Local development adapter. Disabled when ENABLE_DEMO_PAYMENTS is false.
      security:
        - bearerAuth: []
      parameters:
        - name: paymentId
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '200':
          description: The hold was atomically converted into a paid booking
          content:
            application/json:
              schema:
                type: object
                required: [payment, booking]
                properties:
                  payment: {$ref: '#/components/schemas/PaymentIntent'}
                  booking: {$ref: '#/components/schemas/Booking'}
        '409':
          description: Hold expired or booking state changed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/payments/webhooks/sber:
    post:
      tags: [Payments]
      operationId: handleSberWebhook
      description: The callback is never trusted by itself. Rooms queries getOrderStatusExtended server-to-server before changing payment or booking state.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [mdOrder, orderNumber, operation, status]
              properties:
                mdOrder: {type: string, format: uuid}
                orderNumber: {type: string, minLength: 1, maxLength: 36}
                operation:
                  type: string
                  enum: [created, approved, deposited, reversed, refunded, declinedByTimeout, subscriptionCreated]
                status: {type: integer, minimum: 0, maximum: 1}
                additionalParams: {type: object, additionalProperties: true}
      responses:
        '200':
          description: Event verified and processed idempotently
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status: {type: string, const: ok}
        '409':
          description: Provider order or deposited amount does not match the Rooms transaction
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
        '502':
          description: The bank status could not be verified
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}

  /v1/partner/bank-account:
    get:
      tags: [Partner]
      operationId: getPartnerBankAccount
      description: Returns only masked requisites. The full settlement account is never exposed by the API.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Masked bank account assigned to the partner venue
          content:
            application/json:
              schema: {$ref: '#/components/schemas/BankAccount'}
        '404': {$ref: '#/components/responses/NotFound'}
    put:
      tags: [Partner]
      operationId: updatePartnerBankAccount
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PartnerBankAccountWrite'}
      responses:
        '200':
          description: Requisites encrypted at rest and returned in masked form; verification is reset
          content:
            application/json:
              schema: {$ref: '#/components/schemas/BankAccount'}
        '404': {$ref: '#/components/responses/NotFound'}

  /v1/partner/payouts:
    get:
      tags: [Partner]
      operationId: listPartnerPayouts
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [draft, sent, paid, cancelled, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Payout batches belonging only to the authenticated partner venue
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/PayoutBatch'}

  /v1/accounting/overview:
    get:
      tags: [Accounting]
      operationId: getFinanceOverview
      description: Available to accountant and admin roles.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Aggregated payout and refund amounts
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FinanceOverview'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /v1/accounting/receipts:
    get:
      tags: [Accounting]
      operationId: listFiscalReceipts
      description: Durable online-cash-register queue and fiscal receipt history. Available to accountant and admin roles.
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [queued, processing, succeeded, failed, cancelled, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Fiscal receipt queue and history
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/FiscalReceipt'}
        '403': {$ref: '#/components/responses/Forbidden'}

  /v1/accounting/receipts/{receiptId}/retry:
    post:
      tags: [Accounting]
      operationId: retryFiscalReceipt
      description: Requeues a failed or manually cancelled fiscal receipt and resets its attempt counter. The action is audited.
      security:
        - bearerAuth: []
      parameters:
        - name: receiptId
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '202':
          description: Receipt returned to the cash-register queue
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FiscalReceiptAction'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}

  /v1/accounting/receipts/{receiptId}/cancel:
    post:
      tags: [Accounting]
      operationId: cancelFiscalReceipt
      description: Cancels a queued or failed receipt before it is claimed by a cash-register worker. The action is audited.
      security:
        - bearerAuth: []
      parameters:
        - name: receiptId
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '200':
          description: Receipt job cancelled
          content:
            application/json:
              schema: {$ref: '#/components/schemas/FiscalReceiptAction'}
        '403': {$ref: '#/components/responses/Forbidden'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}

  /v1/accounting/bank-accounts:
    get:
      tags: [Accounting]
      operationId: listVenueBankAccounts
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [pending, verified, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Masked venue bank requisites
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/BankAccount'}

  /v1/accounting/bank-accounts/{venueId}/verify:
    post:
      tags: [Accounting]
      operationId: verifyVenueBankAccount
      security:
        - bearerAuth: []
      parameters:
        - name: venueId
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '200':
          description: Requisites verified and venue payouts unblocked
          content:
            application/json:
              schema: {$ref: '#/components/schemas/BankAccount'}
        '404': {$ref: '#/components/responses/NotFound'}

  /v1/accounting/refunds:
    get:
      tags: [Accounting]
      operationId: listRefunds
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [refund_pending, refunded, failed, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Refund queue and history
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/Refund'}

  /v1/accounting/refunds/{refundId}/complete:
    post:
      tags: [Accounting]
      operationId: completeRefund
      deprecated: true
      description: Manual completion for the local demo provider only. Live Sber refunds are completed exclusively by the refund worker after bank verification.
      security:
        - bearerAuth: []
      parameters:
        - name: refundId
          in: path
          required: true
          schema: {type: string, format: uuid}
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                providerOperationId: {type: string, minLength: 3, maxLength: 200}
      responses:
        '200':
          description: Refund completed idempotently and client notification queued
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Refund'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}

  /v1/accounting/refunds/{refundId}/retry:
    post:
      tags: [Accounting]
      operationId: retryRefund
      description: Requeues a failed automatic refund. A previously submitted refund is checked at the bank and is not submitted twice.
      security:
        - bearerAuth: []
      parameters:
        - name: refundId
          in: path
          required: true
          schema: {type: string, format: uuid}
      responses:
        '202':
          description: Refund returned to the automatic queue
          content:
            application/json:
              schema:
                type: object
                required: [id, status]
                properties:
                  id: {type: string, format: uuid}
                  status: {type: string, const: refund_pending}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}

  /v1/accounting/payout-candidates:
    get:
      tags: [Accounting]
      operationId: listPayoutCandidates
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Completed paid bookings with payout readiness or a blocking reason
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/PayoutCandidate'}

  /v1/accounting/payouts:
    get:
      tags: [Accounting]
      operationId: listPayouts
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [draft, sent, paid, cancelled, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Cross-venue payout batches
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/PayoutBatch'}
    post:
      tags: [Accounting]
      operationId: createPayouts
      description: Groups eligible bookings by venue and creates sent payout batches atomically.
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                bookingIds:
                  type: array
                  minItems: 1
                  maxItems: 200
                  uniqueItems: true
                  items: {type: string, format: uuid}
                scheduledFor: {type: string, format: date}
      responses:
        '201':
          description: Payout batches created and partner notifications queued
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/PayoutBatch'}
        '409': {$ref: '#/components/responses/Conflict'}

  /v1/accounting/payouts/{payoutId}/complete:
    post:
      tags: [Accounting]
      operationId: completePayout
      security:
        - bearerAuth: []
      parameters:
        - name: payoutId
          in: path
          required: true
          schema: {type: string, format: uuid}
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                providerOperationId: {type: string, minLength: 3, maxLength: 200}
      responses:
        '200':
          description: Payout completed idempotently and partner notification queued
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PayoutBatch'}
        '404': {$ref: '#/components/responses/NotFound'}
        '409': {$ref: '#/components/responses/Conflict'}

  /v1/partner/moderation:
    get:
      tags: [Partner]
      operationId: listPartnerModerationHistory
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Moderation history for the venue assigned to the partner
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminModerationRecord'

  /v1/partner/venue:
    get:
      tags: [Partner]
      operationId: getPartnerVenue
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Venue managed by the partner
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartnerVenue'
    patch:
      tags: [Partner]
      operationId: updatePartnerVenue
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PartnerVenueWrite'
      responses:
        '202':
          description: Immediate fields applied and critical fields sent to moderation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartnerVenue'

  /v1/partner/rooms:
    get:
      tags: [Partner]
      operationId: listPartnerRooms
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Partner rooms
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PartnerRoom'
    post:
      tags: [Partner]
      operationId: createPartnerRoom
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PartnerRoomWrite'
      responses:
        '202':
          description: Room draft created and sent to moderation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartnerRoom'

  /v1/partner/rooms/{roomId}:
    patch:
      tags: [Partner]
      operationId: updatePartnerRoom
      security:
        - bearerAuth: []
      parameters:
        - {name: roomId, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PartnerRoomWrite'
      responses:
        '200':
          description: Operational settings applied and public changes sent to moderation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartnerRoom'

  /v1/partner/schedule-exceptions/{date}:
    put:
      tags: [Partner]
      operationId: setPartnerScheduleException
      security:
        - bearerAuth: []
      parameters:
        - {name: date, in: path, required: true, schema: {type: string, format: date}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PartnerScheduleExceptionWrite'
      responses:
        '200':
          description: Date-specific schedule saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartnerVenue'
    delete:
      tags: [Partner]
      operationId: deletePartnerScheduleException
      security:
        - bearerAuth: []
      parameters:
        - {name: date, in: path, required: true, schema: {type: string, format: date}}
      responses:
        '200':
          description: Weekly schedule restored for the date
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartnerVenue'

  /v1/partner/reservations:
    get:
      tags: [Partner]
      operationId: listPartnerReservations
      security:
        - bearerAuth: []
      parameters:
        - name: roomId
          in: query
          schema: {type: string, format: uuid}
        - name: dateFrom
          in: query
          schema: {type: string, format: date}
        - name: dateTo
          in: query
          schema: {type: string, format: date}
        - name: includeCancelled
          in: query
          schema: {type: boolean, default: true}
      responses:
        '200':
          description: Manual bookings and technical blocks belonging to the current partner
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/PartnerReservation'}
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Partner]
      operationId: createPartnerReservation
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PartnerReservationWrite'
      responses:
        '201':
          description: Manual booking or technical block created immediately
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerReservation'}
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/SlotConflict'

  /v1/partner/reservations/{reservationId}:
    patch:
      tags: [Partner]
      operationId: updatePartnerReservation
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ReservationId'
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PartnerReservationWrite'}
      responses:
        '200':
          description: Updated calendar record
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerReservation'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SlotConflict'
    delete:
      tags: [Partner]
      operationId: deleteTechnicalReservation
      description: Permanently deletes a technical block. Manual bookings must be cancelled instead.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ReservationId'
      responses:
        '204':
          description: Technical block deleted
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/partner/reservations/{reservationId}/cancel:
    post:
      tags: [Partner]
      operationId: cancelPartnerReservation
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ReservationId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason: {type: string, minLength: 3, maxLength: 1000}
      responses:
        '200':
          description: Calendar record cancelled and its time released
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerReservation'}
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/partner/reservations/{reservationId}/restore:
    post:
      tags: [Partner]
      operationId: restorePartnerReservation
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ReservationId'
      responses:
        '200':
          description: Cancelled calendar record restored after a fresh availability check
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerReservation'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/SlotConflict'

  /v1/partner/bookings:
    get:
      tags: [Partner]
      operationId: listPartnerBookings
      security:
        - bearerAuth: []
      parameters:
        - name: statusGroup
          in: query
          schema: {type: string, enum: [new, payment, booked, history, all], default: all}
      responses:
        '200':
          description: Bookings belonging only to venues managed by the current partner
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Booking'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/partner/support:
    get:
      tags: [Partner]
      operationId: listPartnerSupportCases
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [open, working, closed, all]}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Support cases for venues managed by the current partner
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/SupportCase'}
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/partner/reviews:
    get:
      tags: [Partner]
      operationId: listPartnerReviews
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Published reviews for venues managed by the current partner
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/ReviewRecord'}
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/partner/reviews/{reviewId}/reply:
    patch:
      tags: [Partner]
      operationId: replyToPartnerReview
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ReviewId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [body]
              properties:
                body: {type: string, minLength: 3, maxLength: 1000}
      responses:
        '200':
          description: Public partner reply saved
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ReviewRecord'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/partner/bookings/{bookingId}/complete:
    post:
      tags: [Partner]
      operationId: completePartnerBooking
      description: Marks a paid booking as visited after its end time and unlocks the client review.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      responses:
        '200':
          description: Booking marked as visited
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Booking'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/partner/bookings/{bookingId}/confirm:
    post:
      tags: [Partner]
      operationId: confirmPartnerBooking
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      responses:
        '200':
          description: Slot held for 15 minutes awaiting payment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
        '409':
          $ref: '#/components/responses/SlotConflict'

  /v1/partner/bookings/{bookingId}/reject:
    post:
      tags: [Partner]
      operationId: rejectPartnerBooking
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [reason]
              properties:
                reason: {type: string, minLength: 5, maxLength: 1000}
      responses:
        '200':
          description: Request rejected and any unpaid hold released
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: Booking status no longer allows rejection

  /v1/partner/bookings/{bookingId}/proposal:
    post:
      tags: [Partner]
      operationId: proposeBookingTime
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimeProposal'
      responses:
        '200':
          description: Alternative time sent to client
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Booking'
        '409':
          description: Booking state changed or the proposed slot is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/admin/two-factor-recovery:
    get:
      tags: [Admin]
      operationId: listTwoFactorRecoveryRequests
      description: Lists the protected-role recovery queue. Expired pending records are closed before the response.
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [pending, approved, rejected, expired, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Recovery review queue
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/TwoFactorRecoveryRecord'}
        '403':
          $ref: '#/components/responses/Forbidden'

  /v1/admin/two-factor-recovery/{recoveryId}:
    patch:
      tags: [Admin]
      operationId: decideTwoFactorRecoveryRequest
      description: A different administrator approves or rejects the request. Approval atomically deletes the old factor, consumes pending challenges, revokes every target session and requires enrollment after the next password login.
      security:
        - bearerAuth: []
      parameters:
        - {name: recoveryId, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [status]
              properties:
                status: {type: string, enum: [approved, rejected]}
                comment: {type: string, maxLength: 1000, description: Required when rejected}
      responses:
        '200':
          description: Audited recovery decision
          content:
            application/json:
              schema: {$ref: '#/components/schemas/TwoFactorRecoveryRecord'}
        '400':
          $ref: '#/components/responses/ValidationError'
        '403':
          description: Administrator role required or self-approval attempted
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/admin/partner-leads:
    get:
      tags: [Admin]
      operationId: listAdminPartnerLeads
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [new, review, approved, rejected, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Venue onboarding queue including contacts and consent evidence
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/PartnerLead'}
        '403':
          $ref: '#/components/responses/Forbidden'

  /v1/admin/partner-leads/{leadId}:
    patch:
      tags: [Admin]
      operationId: decideAdminPartnerLead
      security:
        - bearerAuth: []
      parameters:
        - {name: leadId, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [status]
              properties:
                status: {type: string, enum: [review, approved, rejected]}
                comment: {type: string, maxLength: 1000, description: Required when rejected}
      responses:
        '200':
          description: Queue status and audit record updated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerLead'}
        '400':
          $ref: '#/components/responses/ValidationError'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: A final decision was already recorded
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}

  /v1/admin/partner-leads/{leadId}/invitations:
    post:
      tags: [Admin]
      operationId: issuePartnerInvitation
      security:
        - bearerAuth: []
      description: Issues a 24-hour one-time activation link for an approved lead, revokes any previous unused link and queues an encrypted email delivery.
      parameters:
        - {name: leadId, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        '201':
          description: Raw activation token is returned only inside this one-time URL; only its SHA-256 hash is stored
          headers:
            Cache-Control: {schema: {type: string, const: no-store}}
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PartnerInvitationReceipt'}
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Lead is not approved or the partner account already exists
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}

  /v1/admin/moderation:
    get:
      tags: [Admin]
      operationId: listAdminModeration
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [pending, approved, rejected, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Moderation queue and recent decisions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminModerationRecord'
        '403':
          description: Administrator role required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/admin/reviews:
    get:
      tags: [Admin]
      operationId: listAdminReviews
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [pending, approved, rejected, all], default: all}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Review moderation queue and recent decisions
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/ReviewRecord'}
        '403':
          $ref: '#/components/responses/Unauthorized'

  /v1/admin/reviews/{reviewId}:
    patch:
      tags: [Admin]
      operationId: moderateReview
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/ReviewId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [status]
              properties:
                status: {type: string, enum: [pending, approved, rejected]}
                comment: {type: string, maxLength: 1000, description: Required with at least 5 characters when rejected}
      responses:
        '200':
          description: Review moderation status updated and room rating adjusted atomically
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ReviewRecord'}
        '400':
          $ref: '#/components/responses/ValidationError'
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/admin/moderation/{moderationId}/approve:
    post:
      tags: [Admin]
      operationId: approveAdminModeration
      security:
        - bearerAuth: []
      parameters:
        - {name: moderationId, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminModerationDecision'
      responses:
        '200':
          description: Changes applied atomically and moderation completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminModerationRecord'
        '409':
          description: Moderation was already completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/admin/moderation/{moderationId}/reject:
    post:
      tags: [Admin]
      operationId: rejectAdminModeration
      security:
        - bearerAuth: []
      parameters:
        - {name: moderationId, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminModerationDecision'
      responses:
        '200':
          description: Changes rejected and decision exposed to the partner
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminModerationRecord'
        '400':
          description: Rejection reason is missing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Moderation was already completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v1/admin/bookings:
    get:
      tags: [Admin]
      operationId: listAdminBookings
      security:
        - bearerAuth: []
      parameters:
        - {name: statusGroup, in: query, schema: {type: string, enum: [active, completed, cancelled, all], default: all}}
      responses:
        '200':
          description: Cross-venue booking queue with contacts visible to Rooms
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/Booking'}
        '401':
          $ref: '#/components/responses/Unauthorized'

  /v1/admin/bookings/{bookingId}/cancel:
    post:
      tags: [Admin]
      operationId: cancelBookingByAdmin
      description: Cancels the booking atomically, releases inventory and creates a refund review for a paid prepayment.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/BookingId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [reason]
              properties:
                reason: {type: string, minLength: 3, maxLength: 1000}
      responses:
        '200':
          description: Booking cancelled
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Booking'}
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /v1/admin/support:
    get:
      tags: [Admin]
      operationId: listAdminSupportCases
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [open, working, closed, all]}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 200, default: 80}}
      responses:
        '200':
          description: Cross-venue support queue ordered by status and latest activity
          content:
            application/json:
              schema:
                type: array
                items: {$ref: '#/components/schemas/SupportCase'}

  /v1/admin/support/{supportId}:
    patch:
      tags: [Admin]
      operationId: updateAdminSupportStatus
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/SupportId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [status]
              properties:
                status: {type: string, enum: [open, working, closed]}
      responses:
        '200':
          description: Support case status updated
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SupportCase'}
        '404':
          $ref: '#/components/responses/NotFound'

  /v1/admin/notification-deliveries:
    get:
      tags: [Admin]
      operationId: listNotificationDeliveries
      security:
        - bearerAuth: []
      parameters:
        - {name: status, in: query, schema: {type: string, enum: [queued, processing, sent, failed, cancelled]}}
        - {name: channel, in: query, schema: {type: string, enum: [email, telegram]}}
      responses:
        '200':
          description: Delivery queue
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/NotificationDelivery'

  /v1/admin/operations/health:
    get:
      tags: [Admin]
      operationId: getOperationsHealth
      summary: Aggregated operational health without personal data or secrets
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current process, dependency and queue health
          headers:
            Cache-Control:
              schema: {type: string, example: no-store}
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [status, checkedAt, startedAt, uptimeSeconds, dependencies, requests, queues, backup, finance, warnings]
                properties:
                  status: {type: string, enum: [ready, attention]}
                  checkedAt: {type: string, format: date-time}
                  startedAt: {type: string, format: date-time}
                  uptimeSeconds: {type: integer, minimum: 0}
                  dependencies:
                    type: object
                    additionalProperties: false
                    required: [database, notifications, finance, media, payments]
                    properties:
                      database: {type: string, enum: [memory, postgresql]}
                      notifications: {type: string, enum: [memory, postgresql]}
                      finance: {type: string, enum: [memory, postgresql]}
                      media: {type: string, enum: [memory, local, s3]}
                      payments: {type: string, enum: [demo, provider]}
                  requests:
                    type: object
                    additionalProperties: true
                  queues:
                    type: object
                    additionalProperties: true
                  backup:
                    type: object
                    additionalProperties: false
                    required: [status, createdAt, ageHours, sizeBytes]
                    properties:
                      status: {type: string, enum: [not_configured, missing, fresh, stale]}
                      createdAt: {type: [string, 'null'], format: date-time}
                      ageHours: {type: [number, 'null'], minimum: 0}
                      sizeBytes: {type: [integer, 'null'], minimum: 0}
                  finance:
                    type: object
                    additionalProperties: true
                  warnings:
                    type: array
                    items: {type: string}
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /v1/admin/migrations/validate:
    post:
      tags: [Admin]
      operationId: validateMigration
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {type: object, additionalProperties: true}
      responses:
        '200':
          description: Collection counts and validation warnings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MigrationValidation'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    refreshCookie:
      type: apiKey
      in: cookie
      name: rooms_refresh

  parameters:
    RoomId:
      name: roomId
      in: path
      required: true
      description: Permanent UUID or a legacy public slug during frontend migration.
      schema: {type: string}
    BookingId:
      name: bookingId
      in: path
      required: true
      schema: {type: string, format: uuid}
    ReviewId:
      name: reviewId
      in: path
      required: true
      schema: {type: string, format: uuid}
    SupportId:
      name: supportId
      in: path
      required: true
      schema: {type: string, format: uuid}
    ReservationId:
      name: reservationId
      in: path
      required: true
      schema: {type: string, format: uuid}

  responses:
    ValidationError:
      description: Request validation failed
      content:
        application/json:
          schema: {$ref: '#/components/schemas/Error'}
    Unauthorized:
      description: Authentication failed or session expired
      content:
        application/json:
          schema: {$ref: '#/components/schemas/Error'}
    Forbidden:
      description: Authenticated role is not allowed to perform this operation
      content:
        application/json:
          schema: {$ref: '#/components/schemas/Error'}
    NotFound:
      description: Entity not found
      content:
        application/json:
          schema: {$ref: '#/components/schemas/Error'}
    Conflict:
      description: Entity already exists
      content:
        application/json:
          schema: {$ref: '#/components/schemas/Error'}
    RateLimited:
      description: Request rate limit exceeded
      headers:
        Retry-After:
          schema: {type: integer, minimum: 1}
      content:
        application/json:
          schema: {$ref: '#/components/schemas/Error'}
    SlotConflict:
      description: One or more rooms are no longer available
      content:
        application/json:
          schema:
            allOf:
              - $ref: '#/components/schemas/Error'
              - type: object
                properties:
                  alternatives:
                    type: array
                    items: {$ref: '#/components/schemas/AvailabilityWindow'}

  schemas:
    City:
      type: object
      required: [id, name, active]
      properties:
        id: {type: string}
        name: {type: string}
        active: {type: boolean}
        pilot: {type: boolean}

    CityStats:
      type: object
      required: [city, publishedVenues, publishedRooms, activeClientsLabel, audienceStage, updatedAt]
      properties:
        city: {$ref: '#/components/schemas/City'}
        publishedVenues: {type: integer, minimum: 0}
        publishedRooms: {type: integer, minimum: 0}
        activeClientsLabel:
          type: [string, 'null']
          description: Public threshold such as 20+, 50+ or 100+; null while the city is launching.
        audienceStage: {type: string, enum: [launching, growing, established]}
        updatedAt: {type: string, format: date-time}

    PartnerLeadCreate:
      type: object
      additionalProperties: false
      required: [city, venueTitle, address, contactName, contactPhone, contactEmail, venueType, roomCount, comment, legal]
      properties:
        city: {type: string, minLength: 2, maxLength: 120}
        venueTitle: {type: string, minLength: 2, maxLength: 180}
        address: {type: string, minLength: 3, maxLength: 300}
        contactName: {type: string, minLength: 2, maxLength: 120}
        contactPhone: {type: string, minLength: 10, maxLength: 30}
        contactEmail: {type: string, format: email, maxLength: 320}
        venueType: {type: string, minLength: 2, maxLength: 120}
        roomCount: {type: integer, minimum: 1, maximum: 100}
        comment: {type: string, maxLength: 2000}
        legal:
          type: object
          additionalProperties: false
          required: [termsVersion, privacyVersion, termsAccepted, privacyAccepted]
          properties:
            termsVersion: {type: string, minLength: 1, maxLength: 80}
            privacyVersion: {type: string, minLength: 1, maxLength: 80}
            termsAccepted: {type: boolean, const: true}
            privacyAccepted: {type: boolean, const: true}

    PartnerLeadReceipt:
      type: object
      required: [id, status, createdAt]
      properties:
        id: {type: string, format: uuid}
        status: {type: string, const: new}
        createdAt: {type: string, format: date-time}

    PartnerLead:
      type: object
      required: [id, city, venueTitle, address, contactName, contactPhone, contactEmail, venueType, roomCount, comment, status, termsVersion, privacyVersion, consentedAt, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        city: {type: string}
        venueTitle: {type: string}
        address: {type: string}
        contactName: {type: string}
        contactPhone: {type: string}
        contactEmail: {type: string, format: email}
        venueType: {type: string}
        roomCount: {type: integer, minimum: 1}
        comment: {type: string}
        status: {type: string, enum: [new, review, approved, rejected]}
        termsVersion: {type: string}
        privacyVersion: {type: string}
        consentedAt: {type: string, format: date-time}
        requestIp: {type: [string, 'null']}
        requestUserAgent: {type: [string, 'null']}
        reviewedBy: {type: [string, 'null'], format: uuid}
        reviewComment: {type: [string, 'null']}
        reviewedAt: {type: [string, 'null'], format: date-time}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
        invitation:
          oneOf:
            - $ref: '#/components/schemas/PartnerInvitationStatus'
            - type: 'null'

    PartnerInvitationStatus:
      type: object
      required: [id, leadId, expiresAt, consumedAt, revokedAt, createdAt]
      properties:
        id: {type: string, format: uuid}
        leadId: {type: string, format: uuid}
        expiresAt: {type: string, format: date-time}
        consumedAt: {type: [string, 'null'], format: date-time}
        revokedAt: {type: [string, 'null'], format: date-time}
        createdAt: {type: string, format: date-time}

    PartnerInvitationToken:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token:
          type: string
          pattern: '^[A-Za-z0-9_-]{40,100}$'
          writeOnly: true

    PartnerInvitationAcceptance:
      allOf:
        - $ref: '#/components/schemas/PartnerInvitationToken'
        - type: object
          additionalProperties: false
          required: [token, password, legal]
          properties:
            token: {type: string, pattern: '^[A-Za-z0-9_-]{40,100}$', writeOnly: true}
            password: {type: string, minLength: 8, maxLength: 128, writeOnly: true}
            legal:
              type: object
              additionalProperties: false
              required: [termsVersion, privacyVersion, termsAccepted, privacyAccepted]
              properties:
                termsVersion: {type: string, minLength: 1, maxLength: 100}
                privacyVersion: {type: string, minLength: 1, maxLength: 100}
                termsAccepted: {type: boolean, const: true}
                privacyAccepted: {type: boolean, const: true}

    PartnerInvitationPreview:
      type: object
      required: [venueTitle, city, contactName, expiresAt]
      properties:
        venueTitle: {type: string}
        city: {type: string}
        contactName: {type: string}
        expiresAt: {type: string, format: date-time}

    PartnerInvitationReceipt:
      type: object
      required: [invitationId, activationUrl, expiresAt, delivery]
      properties:
        invitationId: {type: string, format: uuid}
        activationUrl: {type: string, format: uri, writeOnly: true}
        expiresAt: {type: string, format: date-time}
        delivery:
          oneOf:
            - $ref: '#/components/schemas/NotificationDelivery'
            - type: 'null'

    PartnerActivationSession:
      allOf:
        - $ref: '#/components/schemas/AuthSession'
        - type: object
          required: [venue]
          properties:
            venue:
              type: object
              required: [id, title]
              properties:
                id: {type: string, format: uuid}
                title: {type: string}

    PartnerActivationChallenge:
      allOf:
        - $ref: '#/components/schemas/TwoFactorChallenge'
        - type: object
          required: [venue]
          properties:
            venue:
              type: object
              required: [id, title]
              properties:
                id: {type: string, format: uuid}
                title: {type: string}

    TwoFactorChallenge:
      type: object
      additionalProperties: false
      required: [twoFactorRequired, setupRequired, challengeToken, expiresIn]
      properties:
        twoFactorRequired: {type: boolean, const: true}
        setupRequired: {type: boolean}
        challengeToken: {type: string, writeOnly: true}
        expiresIn: {type: integer, const: 300, description: Seconds}
        setup:
          type: object
          additionalProperties: false
          required: [secret, otpAuthUrl, qrDataUrl]
          properties:
            secret: {type: string, writeOnly: true, description: Base32 secret shown only during initial setup.}
            otpAuthUrl: {type: string, writeOnly: true}
            qrDataUrl: {type: string, writeOnly: true}

    TwoFactorSession:
      allOf:
        - $ref: '#/components/schemas/AuthSession'
        - type: object
          required: [recoveryCodes, usedRecoveryCode]
          properties:
            recoveryCodes:
              type: [array, 'null']
              description: Eight one-time codes returned only after initial enrollment.
              items: {type: string}
            usedRecoveryCode: {type: boolean}

    TwoFactorStatus:
      type: object
      additionalProperties: false
      required: [required, enabled, enabledAt, recoveryCodesRemaining]
      properties:
        required: {type: boolean}
        enabled: {type: boolean}
        enabledAt: {type: [string, 'null'], format: date-time}
        recoveryCodesRemaining: {type: integer, minimum: 0, maximum: 8}

    TwoFactorRecoveryAccepted:
      type: object
      additionalProperties: false
      required: [accepted, requestId, expiresAt, expiresIn]
      properties:
        accepted: {type: boolean, const: true}
        requestId: {type: string, format: uuid}
        expiresAt: {type: string, format: date-time}
        expiresIn: {type: integer, const: 86400, description: Seconds}

    TwoFactorRecoveryRecord:
      type: object
      additionalProperties: false
      required:
        - id
        - userId
        - status
        - requestIp
        - requestUserAgent
        - expiresAt
        - reviewedBy
        - reviewComment
        - reviewedAt
        - createdAt
        - updatedAt
        - user
      properties:
        id: {type: string, format: uuid}
        userId: {type: string, format: uuid}
        status: {type: string, enum: [pending, approved, rejected, expired]}
        requestIp: {type: [string, 'null']}
        requestUserAgent: {type: [string, 'null']}
        expiresAt: {type: string, format: date-time}
        reviewedBy: {type: [string, 'null'], format: uuid}
        reviewComment: {type: string}
        reviewedAt: {type: [string, 'null'], format: date-time}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
        user:
          oneOf:
            - $ref: '#/components/schemas/User'
            - type: 'null'

    User:
      type: object
      required: [id, role, name, passwordResetRequired]
      properties:
        id: {type: string, format: uuid}
        role: {type: string, enum: [client, partner, admin, accountant]}
        name: {type: string}
        email: {type: [string, 'null'], format: email}
        phone: {type: [string, 'null']}
        city: {type: [string, 'null']}
        passwordResetRequired: {type: boolean}

    ClientProfileUpdate:
      type: object
      required: [name, email, phone, city]
      properties:
        name: {type: string, minLength: 2, maxLength: 100}
        email: {type: string, format: email}
        phone: {type: string}
        city: {type: string}
        currentPassword: {type: string, description: Required only when newPassword is supplied.}
        newPassword: {type: string, minLength: 8, maxLength: 128}

    AuthSession:
      type: object
      required: [user, accessToken, expiresIn]
      properties:
        user: {$ref: '#/components/schemas/User'}
        accessToken: {type: string}
        expiresIn: {type: integer, description: Seconds}

    PasswordResetAccepted:
      type: object
      required: [accepted, expiresIn, message]
      properties:
        accepted: {type: boolean, const: true}
        expiresIn: {type: integer, const: 900}
        message: {type: string}
        demoToken:
          type: string
          description: Present only when EXPOSE_PASSWORD_RESET_TOKEN is explicitly enabled outside production.
        demoResetUrl:
          type: string
          format: uri
          description: Local demonstration link; a production delivery provider must send the link instead.

    UserSession:
      type: object
      required: [id, secondFactorVerified, expiresAt, createdAt, lastSeenAt, current]
      properties:
        id: {type: string, format: uuid}
        userAgent: {type: [string, 'null']}
        ip: {type: [string, 'null']}
        secondFactorVerified: {type: boolean}
        expiresAt: {type: string, format: date-time}
        createdAt: {type: string, format: date-time}
        lastSeenAt: {type: string, format: date-time}
        current: {type: boolean}

    ClientRegistration:
      type: object
      required: [name, email, phone, city, password, legal]
      properties:
        name: {type: string, minLength: 2}
        email: {type: string, format: email}
        phone: {type: string}
        city: {type: string}
        password: {type: string, minLength: 8}
        legal:
          type: object
          required: [termsVersion, privacyVersion, acceptedAt]
          properties:
            termsVersion: {type: string}
            privacyVersion: {type: string}
            acceptedAt: {type: string, format: date-time}

    Venue:
      type: object
      required: [id, slug, title, city, address, publicationStatus, partnerMode]
      properties:
        id: {type: string, format: uuid}
        slug: {type: string}
        title: {type: string}
        city: {type: string}
        address: {type: string}
        latitude: {type: [number, 'null']}
        longitude: {type: [number, 'null']}
        description: {type: [string, 'null']}
        rules: {type: [string, 'null']}
        amenities: {type: array, items: {type: string}}
        publicationStatus: {type: string, enum: [review, published, hidden]}
        partnerMode: {type: string, enum: [catalog, crm]}
        paymentMethods: {type: array, items: {type: string, enum: [card, cash]}}

    PartnerWeekScheduleDay:
      type: object
      required: [weekday, enabled, opensAtHour, closesAtHour]
      properties:
        weekday: {type: integer, minimum: 1, maximum: 7}
        enabled: {type: boolean}
        opensAtHour: {type: number, minimum: 0, maximum: 23.5, multipleOf: 0.5}
        closesAtHour: {type: number, minimum: 0.5, maximum: 30, multipleOf: 0.5}

    PartnerScheduleException:
      type: object
      required: [date, mode, opensAtHour, closesAtHour, note]
      properties:
        date: {type: string, format: date}
        mode: {type: string, enum: [closed, custom]}
        opensAtHour: {type: [number, 'null']}
        closesAtHour: {type: [number, 'null']}
        note: {type: string}

    PartnerModerationChange:
      type: object
      required: [id, fields, beforeData, proposedData, createdAt]
      properties:
        id: {type: string, format: uuid}
        fields: {type: array, items: {type: string}}
        beforeData: {type: object, additionalProperties: true}
        proposedData: {type: object, additionalProperties: true}
        createdAt: {type: string, format: date-time}

    AdminModerationRecord:
      allOf:
        - $ref: '#/components/schemas/PartnerModerationChange'
        - type: object
          required: [targetType, targetId, targetTitle, venueId, venueTitle, status, reviewComment, reviewedAt]
          properties:
            targetType: {type: string, enum: [venue, room]}
            targetId: {type: string, format: uuid}
            targetTitle: {type: string}
            venueId: {type: string, format: uuid}
            venueTitle: {type: string}
            submittedById: {type: [string, 'null'], format: uuid}
            submittedByName: {type: [string, 'null']}
            submittedByEmail: {type: [string, 'null'], format: email}
            status: {type: string, enum: [pending, approved, rejected]}
            reviewComment: {type: [string, 'null']}
            reviewedById: {type: [string, 'null'], format: uuid}
            reviewedAt: {type: [string, 'null'], format: date-time}

    AdminModerationDecision:
      type: object
      additionalProperties: false
      properties:
        comment: {type: string, maxLength: 1000}

    PartnerVenue:
      allOf:
        - $ref: '#/components/schemas/Venue'
        - type: object
          required: [venueType, contactName, contactPhone, contactEmail, weekSchedule, scheduleExceptions]
          properties:
            venueType: {type: string}
            contactName: {type: string}
            contactPhone: {type: string}
            contactEmail: {type: string}
            weekSchedule:
              type: array
              items: {$ref: '#/components/schemas/PartnerWeekScheduleDay'}
            scheduleExceptions:
              type: array
              items: {$ref: '#/components/schemas/PartnerScheduleException'}
            pendingChange:
              oneOf:
                - $ref: '#/components/schemas/PartnerModerationChange'
                - type: 'null'

    PartnerVenueWrite:
      type: object
      additionalProperties: false
      required: [title, city, address, venueType, description, rules, contactName, contactPhone, contactEmail, amenities, paymentMethods, weekSchedule]
      properties:
        title: {type: string, minLength: 2, maxLength: 160}
        city: {type: string, minLength: 2, maxLength: 100}
        address: {type: string, minLength: 3, maxLength: 300}
        venueType: {type: string, minLength: 2, maxLength: 120}
        description: {type: string, minLength: 10, maxLength: 5000}
        rules: {type: string, maxLength: 5000}
        contactName: {type: string, minLength: 2, maxLength: 120}
        contactPhone: {type: string, minLength: 6, maxLength: 30}
        contactEmail: {type: string, maxLength: 320}
        amenities: {type: array, maxItems: 50, items: {type: string}}
        paymentMethods:
          type: array
          minItems: 1
          uniqueItems: true
          items: {type: string, enum: [card, cash]}
        weekSchedule:
          type: array
          minItems: 7
          maxItems: 7
          items: {$ref: '#/components/schemas/PartnerWeekScheduleDay'}

    PartnerScheduleExceptionWrite:
      type: object
      additionalProperties: false
      required: [mode, opensAtHour, closesAtHour, note]
      properties:
        mode: {type: string, enum: [closed, custom]}
        opensAtHour: {type: [number, 'null'], minimum: 0, maximum: 23.5, multipleOf: 0.5}
        closesAtHour: {type: [number, 'null'], minimum: 0.5, maximum: 30, multipleOf: 0.5}
        note: {type: string, maxLength: 500}

    PartnerRoom:
      type: object
      required: [id, slug, venueId, title, subtitle, type, capacityMin, capacityMax, pricePerHour, minimumHours, description, rules, features, tags, photoPaths, services, priceRules, opensAtHour, closesAtHour, bufferMinutes, publicationStatus]
      properties:
        id: {type: string, format: uuid}
        slug: {type: string}
        venueId: {type: string, format: uuid}
        title: {type: string}
        subtitle: {type: string}
        type: {type: string}
        capacityMin: {type: integer, minimum: 1}
        capacityMax: {type: integer, minimum: 1}
        pricePerHour: {type: number, minimum: 0}
        minimumHours: {type: number, minimum: 0.5, multipleOf: 0.5}
        rating: {type: number, minimum: 0, maximum: 5}
        reviewCount: {type: integer, minimum: 0}
        description: {type: string}
        rules: {type: string}
        promotion: {type: [string, 'null']}
        features: {type: array, items: {type: string}}
        tags: {type: array, items: {type: string}}
        photoPaths: {type: array, items: {type: string}}
        services: {type: array, items: {$ref: '#/components/schemas/RoomService'}}
        priceRules: {type: array, items: {$ref: '#/components/schemas/RoomPriceRule'}}
        opensAtHour: {type: number, minimum: 0, maximum: 23.5, multipleOf: 0.5}
        closesAtHour: {type: number, minimum: 0.5, maximum: 30, multipleOf: 0.5}
        bufferMinutes: {type: integer, enum: [0, 15, 30, 45, 60]}
        publicationStatus: {type: string, enum: [review, published, hidden]}
        pendingChange:
          oneOf:
            - $ref: '#/components/schemas/PartnerModerationChange'
            - type: 'null'

    PartnerRoomWrite:
      type: object
      additionalProperties: false
      required: [title, subtitle, type, description, rules, promotion, capacityMin, capacityMax, pricePerHour, minimumHours, bufferMinutes, opensAtHour, closesAtHour, features, tags, services, status]
      properties:
        title: {type: string, minLength: 2, maxLength: 160}
        subtitle: {type: string, minLength: 2, maxLength: 160}
        type: {type: string, minLength: 2, maxLength: 80}
        description: {type: string, minLength: 10, maxLength: 5000}
        rules: {type: string, maxLength: 5000}
        promotion: {type: string, maxLength: 2000}
        capacityMin: {type: integer, minimum: 1, maximum: 1000}
        capacityMax: {type: integer, minimum: 1, maximum: 1000}
        pricePerHour: {type: number, minimum: 0}
        minimumHours: {type: number, minimum: 0.5, maximum: 24, multipleOf: 0.5}
        bufferMinutes: {type: integer, enum: [0, 15, 30, 45, 60]}
        opensAtHour: {type: number, minimum: 0, maximum: 23.5, multipleOf: 0.5}
        closesAtHour: {type: number, minimum: 0.5, maximum: 30, multipleOf: 0.5}
        features: {type: array, maxItems: 50, uniqueItems: true, items: {type: string}}
        tags: {type: array, maxItems: 50, uniqueItems: true, items: {type: string}}
        services:
          type: array
          maxItems: 50
          items:
            type: object
            required: [name, description, price]
            properties:
              id: {type: string}
              name: {type: string}
              description: {type: string}
              price: {type: number, minimum: 0}
        priceRules:
          type: array
          maxItems: 30
          items: {$ref: '#/components/schemas/RoomPriceRuleWrite'}
        status: {type: string, enum: [review, published, hidden]}

    RoomSummary:
      type: object
      required: [id, slug, venue, title, subtitle, type, capacityMax, pricePerHour, minimumHours, photos]
      properties:
        id: {type: string, format: uuid}
        slug: {type: string}
        venue: {$ref: '#/components/schemas/Venue'}
        title: {type: string}
        subtitle: {type: string}
        type: {type: string}
        capacityMin: {type: integer, minimum: 1}
        capacityMax: {type: integer, minimum: 1}
        pricePerHour: {type: number, minimum: 0}
        minimumHours: {type: number, minimum: 0.5, multipleOf: 0.5}
        rating: {type: number, minimum: 0, maximum: 5}
        reviewCount: {type: integer, minimum: 0}
        features: {type: array, items: {type: string}}
        tags: {type: array, items: {type: string}}
        promotion: {type: [string, 'null']}
        photos: {type: array, items: {type: string, format: uri}}
        nearestWindows: {type: array, items: {$ref: '#/components/schemas/AvailabilityWindow'}}

    RoomDetail:
      allOf:
        - $ref: '#/components/schemas/RoomSummary'
        - type: object
          properties:
            description: {type: string}
            rules: {type: string}
            opensAtHour: {type: number, minimum: 0, maximum: 47.5, multipleOf: 0.5}
            closesAtHour: {type: number, minimum: 0, maximum: 48, multipleOf: 0.5}
            bufferMinutes: {type: integer, enum: [0, 15, 30, 45, 60]}
            services: {type: array, items: {$ref: '#/components/schemas/RoomService'}}
            priceRules: {type: array, items: {$ref: '#/components/schemas/RoomPriceRule'}}
            availability: {$ref: '#/components/schemas/AvailabilityResponse'}

    RoomWrite:
      type: object
      required: [title, type, capacityMax, pricePerHour, minimumHours]
      properties:
        title: {type: string, minLength: 2}
        type: {type: string}
        description: {type: string}
        rules: {type: string}
        promotion: {type: string}
        capacityMin: {type: integer, minimum: 1, default: 1}
        capacityMax: {type: integer, minimum: 1}
        pricePerHour: {type: number, minimum: 0}
        minimumHours: {type: number, minimum: 0.5, multipleOf: 0.5}
        bufferMinutes: {type: integer, enum: [0, 15, 30, 45, 60]}
        features: {type: array, items: {type: string}}

    RoomService:
      type: object
      required: [id, name, price]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        description: {type: [string, 'null']}
        price: {type: number, minimum: 0}

    RoomPriceRule:
      type: object
      required: [id, label, weekdays, startsAtHour, endsAtHour, pricePerHour, priority, active]
      properties:
        id: {type: string, format: uuid}
        label: {type: string}
        weekdays: {type: array, minItems: 1, maxItems: 7, uniqueItems: true, items: {type: integer, minimum: 1, maximum: 7}}
        startsAtHour: {type: number, minimum: 0, maximum: 23.5, multipleOf: 0.5}
        endsAtHour: {type: number, minimum: 0.5, maximum: 30, multipleOf: 0.5}
        pricePerHour: {type: number, minimum: 0}
        priority: {type: integer, minimum: 0}
        active: {type: boolean}

    RoomPriceRuleWrite:
      type: object
      additionalProperties: false
      required: [label, weekdays, startsAtHour, endsAtHour, pricePerHour, active]
      properties:
        id: {type: string}
        label: {type: string, minLength: 1, maxLength: 160}
        weekdays: {type: array, minItems: 1, maxItems: 7, uniqueItems: true, items: {type: integer, minimum: 1, maximum: 7}}
        startsAtHour: {type: number, minimum: 0, maximum: 23.5, multipleOf: 0.5}
        endsAtHour: {type: number, minimum: 0.5, maximum: 30, multipleOf: 0.5}
        pricePerHour: {type: number, minimum: 0}
        active: {type: boolean}

    RoomSearchResult:
      type: object
      required: [items, hasMore]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/RoomSummary'}}
        nextCursor: {type: [string, 'null']}
        hasMore: {type: boolean}

    PublicReview:
      type: object
      required: [id, roomId, authorName, rating, publishedAt]
      properties:
        id: {type: string, format: uuid}
        roomId: {type: string, format: uuid}
        authorName: {type: string, description: First name or a generic privacy-safe label}
        rating: {type: integer, minimum: 1, maximum: 5}
        body: {type: [string, 'null']}
        partnerReply: {type: [string, 'null']}
        publishedAt: {type: string, format: date-time}

    PublicReviewPage:
      type: object
      required: [items, hasMore]
      properties:
        items: {type: array, items: {$ref: '#/components/schemas/PublicReview'}}
        nextCursor: {type: [string, 'null']}
        hasMore: {type: boolean, const: false}

    ReviewSubmit:
      type: object
      additionalProperties: false
      required: [roomId, rating, body]
      properties:
        roomId: {type: string, format: uuid}
        rating: {type: integer, minimum: 1, maximum: 5}
        body: {type: string, minLength: 10, maxLength: 3000}

    ReviewRecord:
      type: object
      required: [id, bookingId, roomId, roomSlug, roomTitle, venueId, venueTitle, authorName, rating, body, status, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        bookingId: {type: string, format: uuid}
        roomId: {type: string, format: uuid}
        roomSlug: {type: string}
        roomTitle: {type: string}
        venueId: {type: string, format: uuid}
        venueTitle: {type: string}
        clientId: {type: [string, 'null'], format: uuid}
        authorName: {type: string}
        rating: {type: integer, minimum: 1, maximum: 5}
        body: {type: string}
        status: {type: string, enum: [pending, approved, rejected]}
        partnerReply: {type: [string, 'null']}
        moderationComment: {type: [string, 'null']}
        moderatedAt: {type: [string, 'null'], format: date-time}
        publishedAt: {type: [string, 'null'], format: date-time}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    AvailabilityRequest:
      type: object
      required: [roomIds, date, durationMinutes]
      properties:
        roomIds:
          type: array
          minItems: 1
          items: {type: string, format: uuid}
        date: {type: string, format: date}
        durationMinutes: {type: integer, minimum: 30, multipleOf: 30}
        preferredTime: {type: [string, 'null']}
        guests: {type: integer, minimum: 1}

    AvailabilityResponse:
      type: object
      required: [date, timezone, windows]
      properties:
        date: {type: string, format: date}
        timezone: {type: string, example: Europe/Moscow}
        windows: {type: array, items: {$ref: '#/components/schemas/AvailabilityWindow'}}

    AvailabilityWindow:
      type: object
      required: [startsAt, maximumDurationMinutes]
      properties:
        startsAt: {type: string, format: date-time}
        maximumDurationMinutes: {type: integer, minimum: 30, multipleOf: 30}
        exactMatch: {type: boolean}

    PlanningPreviewRequest:
      type: object
      additionalProperties: false
      required: [roomSets, date, preferredTime, durationMinutes, guests]
      properties:
        roomSets:
          type: array
          minItems: 1
          maxItems: 20
          items:
            $ref: '#/components/schemas/PlanningRoomSet'
        date: {type: string, format: date}
        preferredTime: {type: string, pattern: '^([01][0-9]|2[0-3]):[0-5][0-9]$'}
        durationMinutes: {type: integer, minimum: 30, maximum: 720, multipleOf: 15}
        guests: {type: integer, minimum: 1, maximum: 1000}
        maxTotalPriceRub: {type: number, minimum: 0, maximum: 100000000}
        maxVariants: {type: integer, minimum: 1, maximum: 20, default: 10}
        maxVariantsPerRoomSet: {type: integer, minimum: 1, maximum: 10, description: Maximum returned start-time variants for each room set.}
        requiredFeatures: {type: array, maxItems: 50, uniqueItems: true, items: {type: string, minLength: 1, maxLength: 100}}
        requestedServiceIds: {type: array, maxItems: 50, uniqueItems: true, items: {type: string, minLength: 1, maxLength: 100}}

    PlanningRoomSet:
      type: object
      additionalProperties: false
      required: [id, roomIds]
      properties:
        id: {type: string, minLength: 1, maxLength: 80, pattern: '^[A-Za-z0-9_-]+$'}
        roomIds:
          type: array
          minItems: 1
          maxItems: 20
          uniqueItems: true
          items: {type: string, minLength: 1, maxLength: 100}

    PlanningPreviewResponse:
      type: object
      required: [algorithmVersion, constraintSetHash, candidatesEvaluated, exactCandidateAvailable, variants, rejections, date, timezone]
      properties:
        algorithmVersion: {type: string, const: 0.2.0}
        constraintSetHash: {type: string, pattern: '^[a-f0-9]{64}$'}
        candidatesEvaluated: {type: integer, minimum: 0}
        exactCandidateAvailable: {type: boolean}
        variants:
          type: array
          maxItems: 20
          items: {$ref: '#/components/schemas/PlanningVariant'}
        rejections:
          type: array
          items: {$ref: '#/components/schemas/PlanningRejection'}
        date: {type: string, format: date}
        timezone: {type: string, const: Europe/Moscow}

    PlanningVariant:
      type: object
      required: [rank, roomSetId, roomIds, startsAt, maximumDurationMinutes, exactStart, totalPriceRub, roomPriceRub, servicesPriceRub, totalCapacity, score, components, reasons]
      properties:
        rank: {type: integer, minimum: 1, maximum: 20}
        roomSetId: {type: string}
        roomIds: {type: array, minItems: 1, items: {type: string}}
        startsAt: {type: string, format: date-time}
        maximumDurationMinutes: {type: integer, minimum: 30}
        exactStart: {type: boolean}
        totalPriceRub: {type: number, minimum: 0}
        roomPriceRub: {type: number, minimum: 0}
        servicesPriceRub: {type: number, minimum: 0}
        totalCapacity: {type: integer, minimum: 1}
        score: {type: number, minimum: 0, maximum: 1}
        components:
          type: object
          required: [timeShift, fragmentation, price, capacitySlack]
          properties:
            timeShift: {$ref: '#/components/schemas/PlanningComponent'}
            fragmentation: {$ref: '#/components/schemas/PlanningComponent'}
            price: {$ref: '#/components/schemas/PlanningComponent'}
            capacitySlack: {$ref: '#/components/schemas/PlanningComponent'}
        reasons:
          type: array
          minItems: 2
          items: {$ref: '#/components/schemas/PlanningReason'}

    PlanningComponent:
      type: object
      required: [raw, normalized, weight, contribution, unit]
      properties:
        raw: {type: number}
        normalized: {type: number, minimum: 0, maximum: 1}
        weight: {type: number, minimum: 0, maximum: 1}
        contribution: {type: number, minimum: 0, maximum: 1}
        unit: {type: string, enum: [minutes, rub, ratio]}

    PlanningReason:
      type: object
      required: [code, value]
      properties:
        code:
          type: string
          enum: [EXACT_START, SHIFTED_START, PRESERVES_SCHEDULE, REDUCES_FRAGMENTATION, CREATES_SHORT_GAP, LOWEST_PRICE, CAPACITY_MATCH, REQUIRED_FEATURES_MATCH, SERVICES_INCLUDED, DYNAMIC_PRICE]
        value: {type: [number, 'null']}

    PlanningRejection:
      type: object
      required: [roomSetId, codes]
      properties:
        roomSetId: {type: string}
        codes:
          type: array
          minItems: 1
          uniqueItems: true
          items: {type: string, enum: [CAPACITY, MINIMUM_DURATION, PRICE_LIMIT, NO_COMMON_WINDOW, REQUIRED_FEATURES, SERVICE_UNAVAILABLE]}

    BookingCreate:
      type: object
      required: [primaryRoomId, roomIds, startsAt, durationMinutes, guests, legal]
      properties:
        primaryRoomId: {type: string, format: uuid}
        roomIds:
          type: array
          minItems: 1
          uniqueItems: true
          items: {type: string, format: uuid}
        startsAt: {type: string, format: date-time}
        durationMinutes: {type: integer, minimum: 30, multipleOf: 30}
        guests: {type: integer, minimum: 1}
        eventType: {type: [string, 'null']}
        eventName: {type: [string, 'null']}
        serviceIds: {type: array, items: {type: string, format: uuid}}
        onSitePaymentMethod: {type: string, enum: [card, cash]}
        comment: {type: string, maxLength: 2000}
        legal:
          type: object
          required: [termsVersion, privacyVersion, acceptedAt]
          properties:
            termsVersion: {type: string}
            privacyVersion: {type: string}
            acceptedAt: {type: string, format: date-time}

    MoneyBreakdown:
      type: object
      required: [roomTotal, serviceTotal, total, prepayment, remainingOnSite, currency]
      properties:
        roomTotal: {type: number}
        serviceTotal: {type: number}
        total: {type: number}
        prepayment: {type: number, description: 30 percent of total}
        remainingOnSite: {type: number}
        currency: {type: string, const: RUB}

    BookingRoom:
      type: object
      required: [id, slug, title, type, capacityMax, pricePerHour, amount, isPrimary, bufferMinutes]
      properties:
        id: {type: string, format: uuid}
        slug: {type: string}
        title: {type: string}
        type: {type: string}
        capacityMax: {type: integer, minimum: 1}
        pricePerHour: {type: number, minimum: 0}
        amount: {type: number, minimum: 0}
        isPrimary: {type: boolean}
        bufferMinutes: {type: integer, minimum: 0, maximum: 60}

    BookingService:
      type: object
      required: [id, name, price, quantity, amount]
      properties:
        id: {type: string, format: uuid}
        name: {type: string}
        description: {type: [string, 'null']}
        price: {type: number, minimum: 0}
        quantity: {type: integer, minimum: 1}
        amount: {type: number, minimum: 0}

    Booking:
      type: object
      required: [id, publicNumber, status, venue, rooms, startsAt, endsAt, guests, money, proposal, unreadMessages, paymentStatus]
      properties:
        id: {type: string, format: uuid}
        publicNumber: {type: string}
        status: {type: string, enum: [pending, proposed, awaiting_payment, paid, expired, cancelled, visited, completed]}
        venue: {$ref: '#/components/schemas/Venue'}
        rooms: {type: array, items: {$ref: '#/components/schemas/BookingRoom'}}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        guests: {type: integer}
        clientName: {type: string}
        clientPhone: {type: [string, 'null'], description: Null for partner before prepayment}
        clientEmail: {type: [string, 'null'], format: email, description: Null for partner before prepayment}
        eventType: {type: [string, 'null']}
        eventName: {type: [string, 'null']}
        onSitePaymentMethod: {type: string, enum: [card, cash]}
        comment: {type: string}
        cancellationReason: {type: [string, 'null']}
        cancelledBy: {type: [string, 'null'], enum: [client, partner, admin, null]}
        services: {type: array, items: {$ref: '#/components/schemas/BookingService'}}
        money: {$ref: '#/components/schemas/MoneyBreakdown'}
        proposal:
          anyOf:
            - $ref: '#/components/schemas/BookingTimeProposal'
            - type: 'null'
        unreadMessages: {type: integer, minimum: 0}
        paymentStatus: {type: string, enum: [unpaid, pending, paid, refund_pending, refunded]}
        paymentHoldExpiresAt: {type: [string, 'null'], format: date-time}
        createdAt: {type: string, format: date-time}

    BookingMessage:
      type: object
      required: [id, senderRole, body, createdAt, readBy]
      properties:
        id: {type: string, format: uuid}
        senderRole: {type: string, enum: [client, partner, admin]}
        body: {type: string}
        createdAt: {type: string, format: date-time}
        readBy:
          type: array
          uniqueItems: true
          items: {type: string, enum: [client, partner]}

    SupportMessage:
      type: object
      required: [id, senderId, senderRole, body, createdAt]
      properties:
        id: {type: string, format: uuid}
        senderId: {type: [string, 'null'], format: uuid}
        senderRole: {type: string, enum: [client, partner, admin]}
        body: {type: string}
        createdAt: {type: string, format: date-time}

    SupportCase:
      type: object
      required: [id, bookingId, publicNumber, venueTitle, roomTitle, startsAt, topic, status, openedBy, openedByRole, assignedTo, closedAt, createdAt, updatedAt, messages]
      properties:
        id: {type: string, format: uuid}
        bookingId: {type: string, format: uuid}
        publicNumber: {type: string}
        venueTitle: {type: string}
        roomTitle: {type: string}
        startsAt: {type: string, format: date-time}
        topic: {type: string}
        status: {type: string, enum: [open, working, closed]}
        openedBy: {type: [string, 'null'], format: uuid}
        openedByRole: {type: string, enum: [client, partner, admin]}
        assignedTo: {type: [string, 'null'], format: uuid}
        closedAt: {type: [string, 'null'], format: date-time}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
        messages:
          type: array
          items: {$ref: '#/components/schemas/SupportMessage'}

    BookingTimeProposal:
      type: object
      required: [id, startsAt, endsAt, durationMinutes, comment, status, money, createdAt, respondedAt]
      properties:
        id: {type: string, format: uuid}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        durationMinutes: {type: integer, minimum: 30, multipleOf: 30}
        comment: {type: string}
        status: {type: string, enum: [pending, accepted, declined, superseded]}
        money: {$ref: '#/components/schemas/MoneyBreakdown'}
        createdAt: {type: string, format: date-time}
        respondedAt: {type: [string, 'null'], format: date-time}

    PartnerReservationWrite:
      type: object
      required: [roomId, startsAt, endsAt, type]
      properties:
        roomId: {type: string, format: uuid}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        type:
          type: string
          enum: [manual_booking, technical]
          description: Immutable after creation; PATCH may only update a record without changing its type.
        category: {type: string, enum: [technical, service, private]}
        clientName: {type: [string, 'null'], minLength: 2, maxLength: 100}
        clientPhone: {type: [string, 'null']}
        guests: {type: [integer, 'null'], minimum: 1}
        amount: {type: number, minimum: 0}
        source: {type: [string, 'null'], enum: [phone, whatsapp, telegram, walk_in, other, null]}
        comment: {type: string, maxLength: 2000}

    PartnerReservation:
      type: object
      required: [id, venueId, roomId, roomSlug, roomTitle, type, status, startsAt, endsAt, bufferMinutes, history, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        venueId: {type: string, format: uuid}
        roomId: {type: string, format: uuid}
        roomSlug: {type: string}
        roomTitle: {type: string}
        type: {type: string, enum: [manual_booking, technical]}
        category: {type: [string, 'null'], enum: [technical, service, private, null]}
        status: {type: string, enum: [active, cancelled]}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        clientName: {type: [string, 'null']}
        clientPhone: {type: [string, 'null']}
        guests: {type: [integer, 'null'], minimum: 1}
        amount: {type: number, minimum: 0}
        source: {type: [string, 'null'], enum: [phone, whatsapp, telegram, walk_in, other, null]}
        comment: {type: string}
        cancellationReason: {type: [string, 'null']}
        bufferMinutes: {type: integer, minimum: 0}
        history:
          type: array
          items:
            type: object
            required: [title, details, created]
            properties:
              title: {type: string}
              details: {type: string}
              created: {type: string, format: date-time}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    TimeProposal:
      type: object
      required: [startsAt, durationMinutes]
      properties:
        startsAt: {type: string, format: date-time}
        durationMinutes: {type: integer, minimum: 30, multipleOf: 30}
        comment: {type: string, maxLength: 1000}

    PaymentIntent:
      type: object
      required: [paymentId, bookingId, status, provider, providerPaymentId, amount, currency, redirectUrl, expiresAt, maskedCard, receiptNumber, receiptUrl, createdAt, paidAt]
      properties:
        paymentId: {type: string, format: uuid}
        bookingId: {type: string, format: uuid}
        status: {type: string, enum: [pending, paid, failed, refunded]}
        provider: {type: string, enum: [rooms_demo, sber]}
        providerPaymentId: {type: string}
        amount: {type: number}
        currency: {type: string, const: RUB}
        redirectUrl: {type: string, format: uri}
        expiresAt: {type: string, format: date-time}
        maskedCard: {type: [string, 'null']}
        receiptNumber: {type: [string, 'null']}
        receiptUrl: {type: [string, 'null'], format: uri}
        createdAt: {type: string, format: date-time}
        paidAt: {type: [string, 'null'], format: date-time}

    FiscalReceipt:
      type: object
      required: [id, paymentId, bookingId, publicNumber, venueTitle, receiptType, status, amount, currency, provider, providerReceiptId, fiscalDocumentNumber, fiscalSign, receiptUrl, attempts, nextAttemptAt, lastError, completedAt, createdAt, updatedAt]
      properties:
        id: {type: string, format: uuid}
        paymentId: {type: string, format: uuid}
        bookingId: {type: string, format: uuid}
        publicNumber: {type: string}
        venueTitle: {type: string}
        receiptType: {type: string, enum: [sale, refund]}
        status: {type: string, enum: [queued, processing, succeeded, failed, cancelled]}
        amount: {type: number, minimum: 0}
        currency: {type: string, const: RUB}
        provider: {type: [string, 'null']}
        providerReceiptId: {type: [string, 'null']}
        fiscalDocumentNumber: {type: [string, 'null']}
        fiscalSign: {type: [string, 'null']}
        receiptUrl: {type: [string, 'null'], format: uri}
        attempts: {type: integer, minimum: 0}
        nextAttemptAt: {type: [string, 'null'], format: date-time}
        lastError: {type: [string, 'null']}
        completedAt: {type: [string, 'null'], format: date-time}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    FiscalReceiptAction:
      type: object
      required: [id, status]
      properties:
        id: {type: string, format: uuid}
        status: {type: string, enum: [queued, cancelled]}

    BankAccount:
      type: object
      description: Masked bank requisites. The full settlement account is encrypted and never returned.
      required: [venueId, venueTitle, bankName, bik, accountLastFour, verifiedAt, updatedAt]
      properties:
        venueId: {type: string, format: uuid}
        venueTitle: {type: string}
        bankName: {type: [string, 'null']}
        bik: {type: [string, 'null'], pattern: '^[0-9]{9}$'}
        accountLastFour: {type: [string, 'null'], pattern: '^[0-9]{4}$'}
        verifiedAt: {type: [string, 'null'], format: date-time}
        updatedAt: {type: [string, 'null'], format: date-time}

    PartnerBankAccountWrite:
      type: object
      additionalProperties: false
      required: [bankName, bik, settlementAccount]
      properties:
        bankName: {type: string, minLength: 2, maxLength: 150}
        bik: {type: string, pattern: '^[0-9]{9}$'}
        settlementAccount: {type: string, pattern: '^[0-9]{20}$'}

    Refund:
      type: object
      required: [id, paymentId, bookingId, publicNumber, venueId, venueTitle, clientName, amount, currency, provider, status, reason, providerRefundId, requestedBy, completedAt, attempts, nextAttemptAt, processingStartedAt, submittedAt, lastError, createdAt]
      properties:
        id: {type: string, format: uuid}
        paymentId: {type: string, format: uuid}
        bookingId: {type: string, format: uuid}
        publicNumber: {type: string}
        venueId: {type: string, format: uuid}
        venueTitle: {type: string}
        clientName: {type: string}
        amount: {type: number, minimum: 0}
        currency: {type: string, const: RUB}
        provider: {type: string}
        status: {type: string, enum: [refund_pending, refunded, failed]}
        reason: {type: string}
        providerRefundId: {type: [string, 'null']}
        requestedBy: {type: [string, 'null'], format: uuid}
        completedAt: {type: [string, 'null'], format: date-time}
        attempts: {type: integer, minimum: 0}
        nextAttemptAt: {type: [string, 'null'], format: date-time}
        processingStartedAt: {type: [string, 'null'], format: date-time}
        submittedAt: {type: [string, 'null'], format: date-time}
        lastError: {type: [string, 'null']}
        createdAt: {type: string, format: date-time}

    PayoutCandidate:
      type: object
      required: [bookingId, publicNumber, venueId, venueTitle, startsAt, endsAt, prepayment, commission, amount, bankAccountVerified, accountLastFour, blockedReason]
      properties:
        bookingId: {type: string, format: uuid}
        publicNumber: {type: string}
        venueId: {type: string, format: uuid}
        venueTitle: {type: string}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        prepayment: {type: number, minimum: 0}
        commission: {type: number, minimum: 0}
        amount: {type: number, minimum: 0, description: Prepayment minus Rooms commission}
        bankAccountVerified: {type: boolean}
        accountLastFour: {type: [string, 'null'], pattern: '^[0-9]{4}$'}
        blockedReason:
          type: [string, 'null']
          enum: [bank_account_missing, bank_account_unverified, support_open, refund_pending, nothing_to_pay, null]

    PayoutItem:
      type: object
      required: [bookingId, publicNumber, startsAt, prepayment, commission, amount]
      properties:
        bookingId: {type: string, format: uuid}
        publicNumber: {type: string}
        startsAt: {type: string, format: date-time}
        prepayment: {type: number, minimum: 0}
        commission: {type: number, minimum: 0}
        amount: {type: number, minimum: 0}

    PayoutBatch:
      type: object
      required: [id, venueId, venueTitle, status, amount, scheduledFor, sentAt, paidAt, providerPayoutId, accountLastFour, createdBy, createdAt, items]
      properties:
        id: {type: string, format: uuid}
        venueId: {type: string, format: uuid}
        venueTitle: {type: string}
        status: {type: string, enum: [draft, sent, paid, cancelled]}
        amount: {type: number, minimum: 0}
        scheduledFor: {type: [string, 'null'], format: date}
        sentAt: {type: [string, 'null'], format: date-time}
        paidAt: {type: [string, 'null'], format: date-time}
        providerPayoutId: {type: [string, 'null']}
        accountLastFour: {type: [string, 'null'], pattern: '^[0-9]{4}$'}
        createdBy: {type: [string, 'null'], format: uuid}
        createdAt: {type: string, format: date-time}
        items:
          type: array
          minItems: 1
          items: {$ref: '#/components/schemas/PayoutItem'}

    FinanceOverview:
      type: object
      required: [expected, ready, sent, paid, refundPending, refunded, candidateCount, readyCount, refundPendingCount]
      properties:
        expected: {type: number, minimum: 0}
        ready: {type: number, minimum: 0}
        sent: {type: number, minimum: 0}
        paid: {type: number, minimum: 0}
        refundPending: {type: number, minimum: 0}
        refunded: {type: number, minimum: 0}
        candidateCount: {type: integer, minimum: 0}
        readyCount: {type: integer, minimum: 0}
        refundPendingCount: {type: integer, minimum: 0}

    NotificationSettings:
      type: object
      required: [siteEnabled, emailEnabled, telegramEnabled]
      properties:
        siteEnabled: {type: boolean, const: true}
        emailEnabled: {type: boolean}
        emailAddress: {type: [string, 'null'], format: email}
        telegramEnabled: {type: boolean}
        telegramChatId: {type: [string, 'null']}

    NotificationDelivery:
      type: object
      required: [id, channel, target, eventKey, title, status, attempts, createdAt]
      properties:
        id: {type: string, format: uuid}
        userId: {type: [string, 'null'], format: uuid}
        venueId: {type: [string, 'null'], format: uuid}
        channel: {type: string, enum: [email, telegram]}
        target: {type: string}
        eventKey: {type: string}
        title: {type: string}
        status: {type: string, enum: [queued, processing, sent, failed, cancelled]}
        attempts: {type: integer, minimum: 0}
        nextAttemptAt: {type: [string, 'null'], format: date-time}
        lastError: {type: [string, 'null']}
        createdAt: {type: string, format: date-time}
        sentAt: {type: [string, 'null'], format: date-time}

    MigrationValidation:
      type: object
      required: [valid, format, version, collections, warnings]
      properties:
        valid: {type: boolean}
        format: {type: string, const: rooms-migration}
        version: {type: integer, const: 1}
        collections: {type: object, additionalProperties: {type: integer}}
        warnings: {type: array, items: {type: string}}

    Error:
      type: object
      required: [code, message, requestId]
      properties:
        code: {type: string}
        message: {type: string}
        requestId: {type: string}
        fields:
          type: object
          additionalProperties: {type: string}
